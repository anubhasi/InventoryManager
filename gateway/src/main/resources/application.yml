spring:
  application:
    name: gateway
  profiles:
    active: native
  config:
    import: optional:configserver:http://localhost:8888
  main:
    web-application-type: reactive
server:
  port : 8222
